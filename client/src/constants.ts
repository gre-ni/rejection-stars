// Product constant: the grid always has exactly 1000 slots.
export const TOTAL_SLOTS = 1000;

// Column count is a layout concern and lives in CSS: see --grid-cols in
// styles/global.css (responsive via media queries, easy to retune from Figma).
